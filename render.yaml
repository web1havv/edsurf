services:
  - type: web
    name: info-reeler
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && python3 run.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /health
    plan: free
